{
  "installCommand": "curl -fsSL https://deno.land/x/install/install.sh | sh",
  "build": {
    "env": {
      "DENO_VERSION": "1.x"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "api/index.ts" }
  ]
}
